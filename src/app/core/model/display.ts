interface IDisplay {
    value: number | string | boolean;
    display: number | string;
}

export class Display implements IDisplay {
    value: number | string | boolean;
    display: number | string;
}